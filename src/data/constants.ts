/** Maximum number of lessons in the application */
export const MAX_LESSONS = 30;

/** Number of weeks in the curriculum */
export const TOTAL_WEEKS = 6;

/** Lessons per week */
export const LESSONS_PER_WEEK = 5;

/** Minimum accuracy percentage to pass pronunciation exercises */
export const PASSING_ACCURACY_THRESHOLD = 40;

/** Number of pronunciation attempts allowed before stopping */
export const MAX_PRONUNCIATION_ATTEMPTS = 3;

/** Number of incorrect attempts before showing the answer (grammar/reading) */
export const MAX_ATTEMPTS_BEFORE_ANSWER = 3;

/** Minimum word count for valid sentences in writing challenges */
export const MIN_SENTENCE_WORD_COUNT = 5;

/** Number of vocabulary words for pronunciation writing challenge */
export const CHALLENGE_WORDS_COUNT = 10;

/** Number of sentences required in writing challenges */
export const REQUIRED_SENTENCES_COUNT = 5;

/** Points awarded per completed lesson */
export const POINTS_PER_LESSON = 100;

/** Speech synthesis rate (0.1 to 10) */
export const SPEECH_RATE = 0.9;
